# AGENTS.md — контекст проекта «Раскатка адресов»

> Этот файл загружается автоматически при каждом старте сессии. Здесь собрана сводка проекта: архитектура, модули, запуск, ключевые решения и история трудностей.

---

## 📌 Проект

**Название:** Раскатка адресов  
**Цель:** Автоматизация сбора, нормализации, валидации и распределения адресных данных с интеграцией Яндекс Карт и Google Sheets.

---

## 🛠 Стек

| Компонент | Технология |
|-----------|------------|
| Backend   | Python 3.11+ (eel, selenium, gspread, requests, transliterate) |
| Frontend  | HTML5 / CSS3 / Vanilla JS |
| Браузер   | Firefox + geckodriver (Selenium) |
| Хранилище| Google Sheets API v4 (Service Account) |
| Утилиты   | Batch / PowerShell для установки |

---

## 📂 Структура проекта

```text
Раскатка адресов/
├── utils/
│   ├── code.py
│   ├── geckodriver.exe
│   ├── raskatka-adressov-591861b40918.json  # service_account key (в .gitignore!)
│   └── run.bat
├── web/
│   ├── index.html
│   ├── script.js
│   └── style.css
├── .opencode/
│   ├── agents/
│   └── memory/
├── Установщик Раскатки.bat
├── Приложение.exe
├── README.md
├── AGENTS.md                     ← этот файл
└── .gitignore
```

---

## 🧩 Модули (вкладки)

| Вкладка        | Функция |
|----------------|---------|
| **Раскатка** | Случайный подбор адресов из листов `Rolling` и `Parser` по фильтрам город/регион/комментарий |
| **Сбор дополнений** | Пакетный сбор транслита, координат, индексов через Яндекс Карты |
| **Дополнение таблицы** | Пошаговый режим: поиск на карте → капча/выбор здания → нормализация → сохранение в `Parser` |

---

## ⚙️ Запуск

1. `Установщик Раскатки.bat` — создаёт `.venv`, устанавливает deps, качает Firefox ≥140, geckodriver v0.34.0 и JSON key.
2. `utils/run.bat` — запускает `code.py` через `.venv`.
3. Открывается UI на `http://localhost:7000` с splash-экраном.

### Ключевые пути в `code.py`
```python
UTILS_DIR = os.path.dirname(os.path.abspath(__file__))
BASE_DIR = os.path.dirname(UTILS_DIR)
JSON_KEY = os.path.join(UTILS_DIR, "raskatka-adressov-591861b40918.json")
GECKO_PATH = os.path.join(UTILS_DIR, "geckodriver.exe")
WEB_PATH = os.path.join(BASE_DIR, "web")
```

### Google Sheets
- Таблица: **«Раскатка адрессов»**
- Листы: `Rolling` (начальная база), `Parser` (накопленная)
- Дубликаты проверяются по столбцу `address` (индекс 4) перед сохранением.

---

## ⚠️ Ключевые решения и ограничения

| Решение / Проблема | Решение |
|--------------------|---------|
| Консоль CMD «прыгала» при старте | Скриттинг через `ctypes.ShowWindow(hwnd, 0)` + неблокирующий `eel.start(block=False)` |
| `print()` не попадал в UI | Обёртка `EelLogger`, перенаправляющая `sys.stdout/stderr` через `eel.addLog` |
| Старые абсолютные пути ломались при перемещении | Перешли на `os.path.dirname(os.path.abspath(__file__))` |
| Ключ Google API был в открытом виде | Перенесён в `.gitignore`, инсталлятор генерирует JSON при установке |
| Лист «База» → «Раскатка адрессов» | Переименование таблицы в коде |
| Нет документации проекта | Добавлены `README.md` + `AGENTS.md` |

---

## 🔒 Безопасность

- **Никогда** не коммитьте `raskatka-adressov-591861b40918.json` или любые приватные ключи!
- `service_account` JSON исключён из Git через `.gitignore`.
- При создании issue/pull request убирайте ключи.

---

## 📝 Текущие задачи (актуально на каждую сессию обновлять)

<!-- Здесь фиксируйте текущие задачи, чтобы не забыть -->

- [x] Добавлена система версионирования (version.json, check_remote_version, zip-обновление)
- [x] Рефакторинг логирования подключения к БД (пошаговый вывод в UI)
- [x] Переделана вкладка «Раскатка»: 5 textarea заменены на HTML-таблицу с обрезкой текста (ellipsis), липким заголовком и подвалом, кнопками копирования столбцов и всей таблицы (TSV)
- [x] Добавлена нормализация названий регионов из Яндекс Карт (6 регионов с «—», «()»)
- [ ] Доработка модуля «Дополнение таблицы» (автопилот, автосохранение)
- [ ] Тестирование пакетного сбора (`sbor_start_func`)
- [ ] Починить баги версионирования (кэш браузера, пустой текст версии)

---

## 🗂 История диалогов и решённые проблемы

> Записывайте сюда ключевые моменты диалогов, чтобы не объяснять повторно

### Сессия 2026-08-31
- **Инициализация проекта:** созданы `README.md`, `AGENTS.md`, структура `.opencode/`.
- **Коммит и push:** `632148f` — splash-экран, стриминг логов, обновление путей; `88d0038` — документация.
- **Репозиторий:** `https://github.com/Miu-kontent/Raskatka`
- **Проблема с памятью:** Gemini исчерпал лимиты → решено хранить контекст локально в `AGENTS.md` + `.opencode/memory/`.

### Сессия 2026-08-31 — этап 3
- **Система версионирования:** убран `updater.bat`, заменён на скачивание zip с GitHub + `os.execl` для перезапуска.
- **Кэш браузера:** добавлен cache-busting (`?v=3`) к `script.js` и `style.css` в `index.html`.
- **Debug-лог:** добавлен детальный вывод в `run_initial_checks()` для диагностики версий.
- **Убран `EelLogger`:** возврат к `eel.addLog()` из Python в JS для стриминга логов.
- **Коммит и push:** `0bfe61c` — debug-лог для диагностики.

### Структура запуска
- `Приложение.exe` — .NET обёртка (`launcher.cs`), запускает `cmd.exe /c utils\run.bat`, тот вызывает `.venv\Scripts\python.exe utils\code.py`
- Код читается с диска, изменения в `code.py` должны применяться после перезапуска
- Для компиляции нового `.exe` используется PowerShell-скрипт с `csc.exe` (.NET 4.0)

---

### Сессия 2026-08-31 — этап 4 (фронтенд + нормализация регионов)
- **Вкладка «Раскатка» — замена textarea на таблицу:**
  - 5 `<textarea>` заменены на единую `<table id="raskatka-results-table">` с `<colgroup>` для управления шириной колонок
  - Добавлены `<thead>` (липкий, z-index: 2, box-shadow вместо border — фикс артефактов при скролле) и `<tfoot>` (липкий к низу, кнопки копирования в ячейках)
  - Текст в ячейках обрезается `text-overflow: ellipsis`, но полностью выделяется мышью
  - Кнопки копирования каждого столбца (5 шт.) и кнопка «Скопировать всю таблицу» (TSV формат для Google Sheets)
  - Ошибки остались в отдельном `textarea#raskatka_errors`
- **Исправления UX копирования:**
  - Копирование столбца теперь сохраняет пустые строки (как TSV вся таблицы)
  - Текст обратной связи унифицирован: «✓ скопировано»
  - Фикс высоты кнопки «Скопировать всю таблицу» (height: 40px, box-sizing: border-box)
- **Python `capture_map_data` — нормализация регионов:**
  - Добавлена функция `normalize_region()` с маппингом 6 регионов:
    - «Кемеровская область — Кузбасс» → «Кемеровская область»
    - «Республика Северная Осетия — Алания» → «Республика Северная Осетия»
    - «Республика Татарстан (Татарстан)» → «Республика Татарстан»
    - «Чувашская Республика — Чувашия» → «Чувашская Республика»
    - «Республика Саха (Якутия)» → «Республика Саха»
    - «Ханты-Мансийский автономный округ — Югра» → «Ханты-Мансийский автономный округ»
  - Вызов перед парсингом региона в `capture_map_data()`
- **Коммиты:** `1b09058` (таблица), `34109a0` (UX копирования), `c2f7053` (нормализация регионов)

## 🧠 Запись новых сессий

При каждом новом диалоге добавляйте сюда блок вида:
```markdown
### Сессия YYYY-MM-DD
- **Задача:** ...
- **Решение:** ...
- **Проблема:** ... → решена через ...
```
